using UnityEngine;
using System.Collections;

public class talkbox : MonoBehaviour {

    public Transform talk;

	// Use this for initialization
	void Start () {

        //var m_font= talk.GetChild(4).GetComponent<font>();
       // m_font.font_list = FIleManager.Getinst().LoadTextData(GUIManager.GetInst().fontPath);

    }
	
	// Update is called once per frame
	void Update () {
	
	}
}
